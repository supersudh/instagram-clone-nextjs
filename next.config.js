/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.jennexplores.com', 'upload.wikimedia.org', 'media.licdn.com', 'images.unsplash.com']
  }
};

module.exports = nextConfig
